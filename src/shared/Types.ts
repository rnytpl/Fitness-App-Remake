export interface ClassType {
  name: string;
  description?: string;
  image: string;
}

export enum SelectedPage {
  Home = "home",
  Benefits = "benefits",
  ContactUs = "contactus",
  OurClasses = "ourclasses",
}
